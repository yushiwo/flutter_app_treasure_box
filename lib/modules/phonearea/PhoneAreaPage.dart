import 'package:flutter/material.dart';

class PhoneAreaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("手机归属地"),
    );
  }

}