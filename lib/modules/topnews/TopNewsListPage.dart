import 'package:flutter/material.dart';

class TopNewsListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("新闻头条"),
    );
  }
  
}